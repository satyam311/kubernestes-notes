# Service Account 

A ServiceAccount (SA) in Kubernetes is essentially a non-human identity used by processes running inside your Pods (your workloads) to interact with the Kubernetes API server and often, external services.

doc : https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/
